#pragma once

namespace app { namespace bin {

const char* version();
const char* num_commits();
const char* date();
const char* variant();
const char* branch();
const char* ahead_by();
const char* num_untracked();
const char* user();
const char* hostname();

}}
